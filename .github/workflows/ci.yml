name: Continuous Integration

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  changes:
    name: Detect Changed Surfaces
    runs-on: ubuntu-latest
    outputs:
      web: ${{ steps.filter.outputs.web }}
      chat: ${{ steps.filter.outputs.chat }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Filter paths
        id: filter
        uses: dorny/paths-filter@v3
        with:
          filters: |
            web:
              - 'src/**'
              - 'public/**'
              - 'prisma/**'
              - 'package.json'
              - 'package-lock.json'
              - 'tsconfig.json'
              - 'next.config.js'
              - 'tailwind.config.ts'
              - 'postcss.config.js'
              - '.eslintrc.json'
              - 'vitest.config.ts'
              - 'Dockerfile'
              - 'docker-compose.yml'
            chat:
              - 'services/chat/**'
              - 'prisma/**'
              - 'docker-compose.yml'
              - 'Dockerfile.migrate'

  web-ci:
    name: Web App CI
    needs: changes
    if: ${{ github.event_name == 'workflow_dispatch' || needs.changes.outputs.web == 'true' }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint -- --max-warnings 0

      - name: Type Check
        run: npx tsc --noEmit

      - name: Run tests
        run: npm run test

      - name: Generate Prisma Client (JS)
        run: npx prisma generate --generator client

      - name: Build
        run: npm run build

  chat-ci:
    name: Chat Service CI
    needs: changes
    if: ${{ github.event_name == 'workflow_dispatch' || needs.changes.outputs.chat == 'true' }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        working-directory: ./services/chat
        run: |
          pip install -r src/api/requirements.txt
          pip install -r tests/requirements-test.txt
          pip install httpx pytest-cov prisma

      - name: Generate Prisma Client (Python)
        run: |
          prisma-client-py generate --schema=${{ github.workspace }}/prisma/schema.prisma

      - name: Run unit tests
        working-directory: ./services/chat
        run: |
          pytest tests/unit/ --cov=src/api --cov-report=term-missing -v
        env:
          PROJECT_ID: local-project
          REGION: us-central1
